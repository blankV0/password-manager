# ─────────────────────────────────────────────────────────────────────────────
# requirements.server.txt
# Dependências APENAS para o servidor FastAPI (Docker).
# NÃO inclui Tkinter, Twilio, requests — só o que o server precisa.
# ─────────────────────────────────────────────────────────────────────────────

# Runtime do servidor ASGI — [standard] inclui uvloop + httptools (mais rápido)
uvicorn[standard]==0.34.0

# Framework API
fastapi==0.115.12

# Hashing de passwords (Argon2id)
argon2-cffi==23.1.0

# JWT
PyJWT==2.10.1

# Validação de email nos schemas Pydantic
email-validator==2.2.0

# Variáveis de ambiente
python-dotenv==1.2.1

# Pydantic v2 vem automaticamente com fastapi, não precisamos declarar
